## Node app deploy to Netlify

- npm init -y
- npm install express netlify-lambda serverless-http
- dist(index.html), functions(api.js) directory create
- netlify.toml file create
- npm install netlify-cli -g
- netlify dev

### Git repository create and upload

### From netlify (Import an existing project from a Git repository)
